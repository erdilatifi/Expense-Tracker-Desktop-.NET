<UserControl x:Class="FinanceTracker.Views.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">
    <UserControl.Resources>
        <Style x:Key="AccountTileButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccountGridRow" TargetType="{x:Type DataGridRow}">
            <Setter Property="Height" Value="46" />
            <Setter Property="Background" Value="{StaticResource CardBgBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBrush}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentPurpleDimBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AccountGridCell" TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <ContentPresenter VerticalAlignment="Center" Margin="8,0" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="28,24,28,28">
            <Grid.RowDefinitions>
                <RowDefinition Height="52" />
                <RowDefinition Height="18" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="18" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Accounts"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           FontSize="16"
                           FontWeight="SemiBold" />
                <TextBlock Text="Balances and account activity"
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="13"
                           Margin="0,4,0,0" />
            </StackPanel>

            <ItemsControl Grid.Row="2" ItemsSource="{Binding AccountTiles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="258" Margin="0,0,14,14">
                            <Button Command="{Binding DataContext.SelectAccountCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource AccountTileButton}">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAddTile}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Border Style="{StaticResource BaseCard}" Height="150">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition Width="72" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <Border Width="44" Height="44" CornerRadius="22" Background="{Binding ColorBrush}" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Icon}" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="20" Foreground="{StaticResource WhiteBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" Margin="0,14,0,0" />
                                            <TextBlock Text="{Binding BalanceText}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="22" FontWeight="Bold" Margin="0,6,0,0" />
                                            <TextBlock Text="{Binding TrendText}" Foreground="{StaticResource AccentPurpleBrush}" FontSize="12" Margin="0,6,0,0" />
                                        </StackPanel>
                                        <lvc:CartesianChart Grid.Column="1" Width="60" Height="30" Series="{Binding SparklineSeries}" XAxes="{Binding DataContext.HiddenAxes, RelativeSource={RelativeSource AncestorType=UserControl}}" YAxes="{Binding DataContext.HiddenAxes, RelativeSource={RelativeSource AncestorType=UserControl}}" LegendPosition="Hidden" TooltipPosition="Hidden" Background="Transparent" VerticalAlignment="Bottom" IsHitTestVisible="False" />
                                    </Grid>
                                </Border>
                            </Button>

                            <Button Command="{Binding DataContext.AddAccountCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource AccountTileButton}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAddTile}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Border Height="150" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource CardBgBrush}" Padding="16">
                                    <Grid>
                                        <Rectangle Stroke="{StaticResource TextMutedBrush}" StrokeDashArray="4 4" RadiusX="12" RadiusY="12" Fill="Transparent" />
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock Text="+" Foreground="{StaticResource TextMutedBrush}" FontSize="34" FontWeight="Light" HorizontalAlignment="Center" />
                                            <TextBlock Text="Add account" Foreground="{StaticResource TextMutedBrush}" FontSize="13" HorizontalAlignment="Center" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Grid.Row="4" Style="{StaticResource BaseCard}" MinHeight="122">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="14" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <TextBlock Text="Account transactions"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="15"
                                   FontWeight="SemiBold" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12" VerticalAlignment="Center" Margin="0,0,12,0">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="Page {0} of {1}">
                                        <Binding Path="CurrentPage" />
                                        <Binding Path="TotalPages" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <Button Content="&#x2039;"
                                    Style="{StaticResource DarkButton}"
                                    Command="{Binding PreviousPageCommand}"
                                    Width="30" Height="30" Padding="0"
                                    FontSize="16"
                                    Margin="0,0,6,0" />
                            <Button Content="&#x203A;"
                                    Style="{StaticResource DarkButton}"
                                    Command="{Binding NextPageCommand}"
                                    Width="30" Height="30" Padding="0"
                                    FontSize="16" />
                        </StackPanel>
                    </Grid>

                    <DataGrid Grid.Row="2"
                              Style="{StaticResource DarkDataGrid}"
                              ItemsSource="{Binding Transactions}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserResizeRows="False"
                              CanUserSortColumns="False"
                              SelectionMode="Single"
                              RowHeight="46"
                              ColumnHeaderHeight="34"
                              GridLinesVisibility="None"
                              BorderThickness="0"
                              Background="Transparent"
                              RowHeaderWidth="0"
                              RowStyle="{StaticResource AccountGridRow}"
                              CellStyle="{StaticResource AccountGridCell}">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Padding" Value="8,0" />
                                <Setter Property="FontSize" Value="11" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="DATE" Binding="{Binding DateText}" Width="90" IsReadOnly="True" />
                            <DataGridTextColumn Header="DESCRIPTION" Binding="{Binding Description}" Width="*" IsReadOnly="True" />
                            <DataGridTemplateColumn Header="CATEGORY" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="8" Height="8" Fill="{Binding CategoryBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                            <TextBlock Text="{Binding CategoryName}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="AMOUNT" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding AmountText}" Foreground="{Binding AmountBrush}" FontSize="13" FontWeight="Bold" HorizontalAlignment="Right" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
