<UserControl x:Class="FinanceTracker.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:FinanceTracker.Converters">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:StringEqualityConverter x:Key="StringEqualityConverter" />
        <Style x:Key="SettingsPanel" TargetType="{x:Type Grid}">
            <Setter Property="Visibility" Value="Collapsed" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="28,24,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="52" />
            <RowDefinition Height="18" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Settings"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       FontSize="16"
                       FontWeight="SemiBold" />
            <TextBlock Text="Workspace preferences and local data"
                       Foreground="{StaticResource TextMutedBrush}"
                       FontSize="13"
                       Margin="0,4,0,0" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource BaseCard}" Padding="8">
                <ListBox ItemsSource="{Binding Sections}"
                         SelectedItem="{Binding SelectedSection}"
                         Background="Transparent"
                         BorderThickness="0"
                         ItemContainerStyle="{StaticResource SidebarItem}" />
            </Border>

            <Border Grid.Column="2" Style="{StaticResource BaseCard}">
                <Grid>
                    <Grid>
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}" BasedOn="{StaticResource SettingsPanel}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedSection}" Value="General">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <StackPanel Width="420" HorizontalAlignment="Left">
                        <TextBlock Text="General" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,18" />
                        <TextBlock Text="Currency" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8" />
                        <ComboBox Style="{StaticResource DarkComboBox}" ItemsSource="{Binding Currencies}" SelectedItem="{Binding Currency}" Height="38" />
                        <TextBlock Text="Date format" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,18,0,8" />
                        <ComboBox Style="{StaticResource DarkComboBox}" ItemsSource="{Binding DateFormats}" SelectedItem="{Binding DateFormat}" Height="38" />
                        <TextBlock Text="First day of week" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,18,0,8" />
                        <ComboBox Style="{StaticResource DarkComboBox}" ItemsSource="{Binding WeekDays}" SelectedItem="{Binding FirstDayOfWeek}" Height="38" />
                        <TextBlock Text="Accent color" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,18,0,10" />
                        <ItemsControl ItemsSource="{Binding AccentSwatches}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Width="36" Height="36" Margin="0,0,8,8" Padding="0"
                                            Command="{Binding DataContext.SelectAccentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="{Binding Name}"
                                            BorderThickness="0"
                                            Background="{Binding Brush}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="SwatchBorder"
                                                        CornerRadius="10"
                                                        Background="{TemplateBinding Background}"
                                                        Width="36" Height="36"
                                                        BorderThickness="0"
                                                        BorderBrush="Transparent">
                                                    <TextBlock x:Name="Check"
                                                               Text="&#xE73E;"
                                                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                                               Foreground="White"
                                                               FontSize="13"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center">
                                                        <TextBlock.Visibility>
                                                            <MultiBinding Converter="{StaticResource StringEqualityConverter}">
                                                                <Binding Path="DataContext.AccentColor" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                                                <Binding Path="ColorHex" />
                                                            </MultiBinding>
                                                        </TextBlock.Visibility>
                                                    </TextBlock>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="SwatchBorder" Property="Opacity" Value="0.8" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    </Grid>

                    <Grid>
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}" BasedOn="{StaticResource SettingsPanel}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedSection}" Value="Accounts">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <TextBlock Text="Accounts" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="SemiBold" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Save" Style="{StaticResource DarkButton}" Command="{Binding SaveAccountsCommand}" Margin="0,0,8,0" />
                                <Button Content="Add Account" Style="{StaticResource AccentButton}" Command="{Binding AddAccountCommand}" />
                            </StackPanel>
                        </Grid>
                        <DataGrid Grid.Row="2" ItemsSource="{Binding AccountRows}" Style="{StaticResource DarkDataGrid}" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                                <DataGridTextColumn Header="Balance" Binding="{Binding Balance}" Width="90" IsReadOnly="True" />
                                <DataGridTextColumn Header="Opening" Binding="{Binding OpeningBalance}" Width="90" />
                                <DataGridTextColumn Header="Credit limit" Binding="{Binding CreditLimit}" Width="90" />
                                <DataGridTextColumn Header="Color" Binding="{Binding ColorHex}" Width="90" />
                                <DataGridComboBoxColumn Header="Type" SelectedItemBinding="{Binding Type}" ItemsSource="{Binding DataContext.AccountTypes, RelativeSource={RelativeSource AncestorType=UserControl}}" Width="120" />
                                <DataGridTemplateColumn Header="" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Delete" Style="{StaticResource DarkButton}" Command="{Binding DataContext.DeleteAccountCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="8,4" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                    </Grid>

                    <Grid>
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}" BasedOn="{StaticResource SettingsPanel}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedSection}" Value="Categories">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <TextBlock Text="Categories" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="SemiBold" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Save" Style="{StaticResource DarkButton}" Command="{Binding SaveCategoriesCommand}" Margin="0,0,8,0" />
                                <Button Content="Add Category" Style="{StaticResource AccentButton}" Command="{Binding AddCategoryCommand}" />
                            </StackPanel>
                        </Grid>
                        <DataGrid Grid.Row="2" ItemsSource="{Binding CategoryRows}" Style="{StaticResource DarkDataGrid}" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                                <DataGridTextColumn Header="Used" Binding="{Binding UsageCount}" Width="60" IsReadOnly="True" />
                                <DataGridTextColumn Header="Color" Binding="{Binding ColorHex}" Width="90" />
                                <DataGridComboBoxColumn Header="Icon" SelectedItemBinding="{Binding IconCode}" ItemsSource="{Binding DataContext.IconOptions, RelativeSource={RelativeSource AncestorType=UserControl}}" Width="130" />
                                <DataGridComboBoxColumn Header="Type" SelectedItemBinding="{Binding Type}" ItemsSource="{Binding DataContext.CategoryTypes, RelativeSource={RelativeSource AncestorType=UserControl}}" Width="110" />
                                <DataGridTemplateColumn Header="" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Delete" Style="{StaticResource DarkButton}" Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="8,4" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                    </Grid>

                    <Grid>
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}" BasedOn="{StaticResource SettingsPanel}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedSection}" Value="Data">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <StackPanel Width="360" HorizontalAlignment="Left">
                        <TextBlock Text="Data" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,18" />
                        <Button Content="Import CSV" Style="{StaticResource AccentButton}" Command="{Binding OpenImportWizardCommand}" Height="38" Margin="0,0,0,10" />
                        <Button Content="Backup database" Style="{StaticResource DarkButton}" Command="{Binding BackupDatabaseCommand}" Height="38" Margin="0,0,0,10" />
                        <Button Content="Restore database" Style="{StaticResource DarkButton}" Command="{Binding RestoreDatabaseCommand}" Height="38" Margin="0,0,0,10" />
                        <Button Content="Clear all data" Style="{StaticResource AccentButton}" Background="{StaticResource AccentPinkBrush}" BorderBrush="{StaticResource AccentPinkBrush}" Command="{Binding ClearAllDataCommand}" Height="38" />
                    </StackPanel>
                    </Grid>

                    <Grid>
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}" BasedOn="{StaticResource SettingsPanel}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedSection}" Value="About">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <StackPanel>
                        <TextBlock Text="About" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="FinanceTracker local desktop workspace" Foreground="{StaticResource TextMutedBrush}" FontSize="13" Margin="0,12,0,0" />
                        <TextBlock Text="Version 1.0" Foreground="{StaticResource TextMutedBrush}" FontSize="13" Margin="0,4,0,0" />
                    </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.RowSpan="3"
                Background="#CC0D0F14"
                Visibility="{Binding IsImportWizardOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Width="720"
                    Background="{StaticResource CardBgBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="18"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="14" />
                        <RowDefinition Height="320" />
                        <RowDefinition Height="14" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <TextBlock Text="Import CSV" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="SemiBold" />
                        <Button Content="×" Style="{StaticResource DarkButton}" Command="{Binding CloseImportWizardCommand}" Width="32" Height="32" Padding="0" HorizontalAlignment="Right" />
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.Style>
                            <Style TargetType="{x:Type Grid}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ImportStep}" Value="1">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="12" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Button Content="Choose CSV file" Style="{StaticResource AccentButton}" Command="{Binding PickImportFileCommand}" HorizontalAlignment="Left" />
                            <DataGrid Grid.Row="2" ItemsSource="{Binding PreviewRows}" Style="{StaticResource DarkDataGrid}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Column 1" Binding="{Binding Column1}" Width="*" />
                                    <DataGridTextColumn Header="Column 2" Binding="{Binding Column2}" Width="*" />
                                    <DataGridTextColumn Header="Column 3" Binding="{Binding Column3}" Width="*" />
                                    <DataGridTextColumn Header="Column 4" Binding="{Binding Column4}" Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>

                    <Grid Grid.Row="2" Width="520" HorizontalAlignment="Left">
                        <Grid.Style>
                            <Style TargetType="{x:Type Grid}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ImportStep}" Value="2">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        
                        <!-- Column 1 -->
                        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,12">
                            <TextBlock Text="Date column" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8" />
                            <ComboBox Style="{StaticResource DarkComboBox}" ItemsSource="{Binding DetectedColumns}" SelectedItem="{Binding DateColumn}" Height="36" />
                        </StackPanel>
                        
                        <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,12">
                            <TextBlock Text="Amount column" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8" />
                            <ComboBox Style="{StaticResource DarkComboBox}" ItemsSource="{Binding DetectedColumns}" SelectedItem="{Binding AmountColumn}" Height="36" />
                        </StackPanel>

                        <StackPanel Grid.Column="0" Grid.Row="2">
                            <TextBlock Text="Description column" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8" />
                            <ComboBox Style="{StaticResource DarkComboBox}" ItemsSource="{Binding DetectedColumns}" SelectedItem="{Binding DescriptionColumn}" Height="36" />
                        </StackPanel>

                        <!-- Column 2 -->
                        <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,12">
                            <TextBlock Text="Category column" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8" />
                            <ComboBox Style="{StaticResource DarkComboBox}" ItemsSource="{Binding DetectedColumns}" SelectedItem="{Binding CategoryColumn}" Height="36" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,12">
                            <TextBlock Text="Account column" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8" />
                            <ComboBox Style="{StaticResource DarkComboBox}" ItemsSource="{Binding DetectedColumns}" SelectedItem="{Binding AccountColumn}" Height="36" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" Grid.Row="2">
                            <TextBlock Text="Type column (Optional)" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8" />
                            <ComboBox Style="{StaticResource DarkComboBox}" ItemsSource="{Binding DetectedColumns}" SelectedItem="{Binding TypeColumn}" Height="36" />
                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <StackPanel.Style>
                            <Style TargetType="{x:Type StackPanel}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ImportStep}" Value="3">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="{Binding ImportSummary}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" />
                        <Button Content="Import" Style="{StaticResource AccentButton}" Command="{Binding ConfirmImportCommand}" Width="120" Height="38" Margin="0,18,0,0" HorizontalAlignment="Center" />
                    </StackPanel>

                    <Grid Grid.Row="4">
                        <Button Content="Back" Style="{StaticResource DarkButton}" Command="{Binding PreviousImportStepCommand}" Width="90" HorizontalAlignment="Left" />
                        <Button Content="Next" Style="{StaticResource AccentButton}" Command="{Binding NextImportStepCommand}" Width="90" HorizontalAlignment="Right" />
                    </Grid>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>
